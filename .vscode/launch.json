{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (lldb)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/PointCloudViewer",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: Build"
        }
    ]
}
